version: "3.9"
services:
 backend:
  build:
   context: ./pizzaria-app/backend
  ports: 
   - "5001:5000"
  networks:
   - pizzaria-network
 frontend:
  build:
   context: ./pizzaria-app/frontend
  ports:
   - "80:80" 
  depends_on:
   - backend
  networks:
   - pizzaria-network
networks:
  pizzaria-network:
